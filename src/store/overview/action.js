import type from './type'

const actions = {
    initTableRows() {
        console.log(type.SET_OVERVIEW_ROWS)
        // context.commit(type.SET_OVERVIEW_ROWS, data);
    },
};

export default actions;